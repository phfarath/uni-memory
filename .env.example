# =============================================================
# Aethera Cortex - Configuracao de Ambiente
# =============================================================
# Copie para .env:  cp .env.example .env
# =============================================================

# --- DATABASE ---
# LOCAL (docker-compose com PostgreSQL local):
DATABASE_URL=postgresql://aethera:aethera_secret@db:5432/aethera_cortex

# CLOUD (Neon.tech ou outro PostgreSQL remoto):
# DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/dbname?sslmode=require

# Schema (use "test" para isolamento de testes)
DB_SCHEMA=public

# --- OPENAI (opcional - necessario apenas para respostas com LLM) ---
OPENAI_API_KEY=

# --- MCP ---
MCP_PUBLIC_URL=http://127.0.0.1:8001

# --- POSTGRES (usado pelo servico db no docker-compose) ---
POSTGRES_USER=aethera
POSTGRES_PASSWORD=aethera_secret
POSTGRES_DB=aethera_cortex
