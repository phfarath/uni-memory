# Frontend Dashboard

> **Versão Target:** V2.0+
> **Status:** ⏳ Pendente
> **Owner:** Unassigned
> **Estimativa:** 120h

---

## Descrição

### Problema
Gerenciamento de memórias é apenas via API/CLI:
- Não há interface visual para usuários não-técnicos
- Difícil visualizar knowledge graph
- Analytics de uso não são acessíveis
- Marketing: Difícil demonstrar valor do produto

### Solução
Criar **Dashboard React/Next.js** para gestão visual de memórias.

**Páginas principais:**
1. **Login** - OAuth 2.1 authentication
2. **Memories Timeline** - Lista cronológica de memórias
3. **Knowledge Graph Visualization** - Grafo interativo (D3.js)
4. **Usage Analytics** - Gráficos de uso (requests, storage)
5. **Settings** - Gerenciar API keys, tier, auto-capture

**Mockup:**
```
┌─────────────────────────────────────┐
│ 🧠 Aethera Cortex Dashboard         │
├─────────────────────────────────────┤
│ Timeline  |  Graph  |  Analytics    │
├─────────────────────────────────────┤
│                                     │
│  📅 Memories Timeline:              │
│  ┌───────────────────────────┐     │
│  │ [2026-02-03] "João traba...│     │
│  │ [2026-02-02] "Projeto X...│     │
│  └───────────────────────────┘     │
│                                     │
│  🔗 Knowledge Graph:                │
│  [Interactive D3.js visualization]  │
│                                     │
└─────────────────────────────────────┘
```

### Valor
✅ **UX amigável** - Usuários não-técnicos podem usar
✅ **Visualização** - Grafo torna relações visíveis
✅ **Marketing** - Demo visual do produto
✅ **Self-service** - Reduz tickets de suporte

---

## Passos de Implementação

### 1. Código (80h)
- [ ] Setup Next.js + TypeScript + Tailwind CSS:
  ```bash
  npx create-next-app@latest aethera-dashboard --typescript --tailwind
  ```

- [ ] Estrutura de páginas:
  ```
  pages/
    ├── login.tsx          # OAuth login
    ├── dashboard.tsx      # Main dashboard
    ├── timeline.tsx       # Memories timeline
    ├── graph.tsx          # Knowledge graph viz
    ├── analytics.tsx      # Usage stats
    └── settings.tsx       # API keys, config
  ```

- [ ] Integração com REST API:
  ```typescript
  // lib/api.ts
  export async function fetchMemories(apiKey: string) {
      const res = await fetch('http://localhost:8001/v1/memories', {
          headers: { 'x-api-key': apiKey }
      });
      return res.json();
  }
  ```

- [ ] Knowledge Graph visualization com D3.js:
  ```typescript
  import * as d3 from 'd3';

  export function renderGraph(entities, relations) {
      // D3 force-directed graph
      const simulation = d3.forceSimulation(entities)
          .force('link', d3.forceLink(relations))
          .force('charge', d3.forceManyBody())
          .force('center', d3.forceCenter(width / 2, height / 2));
  }
  ```

- [ ] Charts com Recharts:
  ```typescript
  import { LineChart, Line, XAxis, YAxis } from 'recharts';

  <LineChart data={usageData}>
      <Line type="monotone" dataKey="requests" stroke="#8884d8" />
  </LineChart>
  ```

- [ ] OAuth integration (Auth0 ou Clerk)

### 2. Testes (20h)
- [ ] E2E tests com Playwright
- [ ] Component tests com Jest + React Testing Library
- [ ] API integration tests

### 3. Documentação (10h)
- [ ] User guide com screenshots
- [ ] Deployment guide (Vercel/Netlify)

### 4. Infraestrutura (10h)
- [ ] Deploy no Vercel
- [ ] Setup CI/CD (GitHub Actions)
- [ ] Domain e SSL

---

## Dependências
- **Feature 007** (OAuth 2.1) - Necessário para autenticação
- **Feature 002** (Knowledge Graph) - Necessário para visualização de grafo

---

## Referências
- [Next.js Docs](https://nextjs.org/docs)
- [D3.js Force Graph](https://observablehq.com/@d3/force-directed-graph)
- [Recharts](https://recharts.org/)

---

**Versão do documento:** 1.0
**Última atualização:** 2026-02-03
